# Banks update balances at most a few times a day and PSD2 rate-limits
# unattended access (commonly 4 calls per account per day). Twice daily is
# plenty and stays well inside the limit.
[Timer]
OnCalendar=
OnCalendar=*-*-* 07:20
OnCalendar=*-*-* 19:20
