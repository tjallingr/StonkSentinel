# Every 4h on weekdays plus a nightly run. Frequent enough to keep the ~24h
# refresh token alive with a wide margin, which is the real reason for the
# cadence — not price freshness.
[Timer]
OnCalendar=
OnCalendar=Mon..Fri 07,11,15,19,23:05
OnCalendar=Sat,Sun 12:05
