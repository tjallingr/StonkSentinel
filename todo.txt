- use an actual repo and ORM instead of sql as string

- DONE make settings more phone friendly. below 620px each form row becomes a
  labelled card with full-width controls instead of an 11-column squeeze: input
  width goes 23px -> 200px (~2 chars -> ~23). labels come from data-label on each
  cell, mirroring the thead, so there's no second list to keep in sync. form
  controls go to 16px on mobile because iOS Safari zooms the page below that.
  desktop is untouched — every rule is inside the media query. row markup is now
  a jinja macro so the blank "add new" row cannot drift out of field order with
  the populated ones (that would silently misalign the parser's zip()).
- make the otc/financeoverview/assets.toml and settings.toml WRITE as well: i need to be able to modify them from the app through the save button

