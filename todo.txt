- use an actual repo and ORM instead of sql as string

- remove orange text
- make settings more phone friendly
- make the otc/financeoverview/assets.toml and settings.toml WRITE as well: i need to be able to modify them from the app through the save button
- remove projections assumptions field: should be clear from the per-item projections.
- make projections clearer: should be visible what is profits and what is purely added cash. also per-item. also nominal vs real
- ui stylee: less ai vibecoded vibes, more academic. i like the font though. performance is still key, do not create high proceessing power html or css
- DONE new modul: cost analysis. /expenses page + `finoverview expenses`. groups by
  counterparty IBAN, excludes transfers between own accounts. transactions table +
  external_transactions/expenses views. still todo: category rollup if payee-only
  ever stops being enough.
- pie chart: remove the three bars below it
- make gold an etc instead of etf
- add horizon setting: it is fixed at 20yrs now. 